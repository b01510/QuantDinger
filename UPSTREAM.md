# Upstream

This project tracks the upstream QuantDinger repository:

- Repository: https://github.com/OpenByteInc/QuantDinger
- Deployment model: Docker / Docker Compose
- Personal repo purpose: keep local deployment helpers and custom extensions separate from upstream source.

Before upgrading, review upstream release notes and test changes in a non-production environment.
